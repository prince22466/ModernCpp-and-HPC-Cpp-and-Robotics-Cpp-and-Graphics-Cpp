#include<filesystem>

//from Cpp 17, comes std::filesystem,
// which provides facilities to manipulate files and directories,
// such as creating, deleting, copying, moving, and iterating over them. 
// also functions to query file properties, such as size, permissions, and timestamps.

// it is like a wrapper on cmd commands(that do these things), 
// but it is cross-platform and works on Windows, Linux, and macOS.

int main()
{
    return 0;
}


//g++ -std=c++20 .\MordernCpp_Any.cpp -o .\MordernCpp_Any.exe; if ($LASTEXITCODE -eq 0) { .\MordernCpp_Any.exe }
